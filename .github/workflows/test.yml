name: Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: treeform/setup-nim-action@v6
      - run: nim r tests/tests.nim
      - run: nim r tests/test_debugging.nim
      - run: nim r tests/test_stepping.nim
